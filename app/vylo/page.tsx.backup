'use client'

import Link from 'next/link'
import Image from 'next/image'
import { motion, AnimatePresence, useMotionValue, useTransform, useSpring, useInView, useScroll } from 'framer-motion'
import { useEffect, useState, useRef } from 'react'

// Add font - Manrope for elegant mono-family design
if (typeof document !== 'undefined') {
  const manropeLink = document.createElement('link')
  manropeLink.href = 'https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600&display=swap'
  manropeLink.rel = 'stylesheet'
  document.head.appendChild(manropeLink)
}

// Animated Counter Component
function AnimatedCounter({ value, prefix = '', suffix = '', delay = 0 }: { value: number, prefix?: string, suffix?: string, delay?: number }) {
  const ref = useRef(null)
  const isInView = useInView(ref, { once: true, margin: '-100px' })
  const [displayValue, setDisplayValue] = useState(0)

  useEffect(() => {
    if (!isInView) return

    const duration = 2000
    const startTime = Date.now() + delay * 1000
    const isDecimal = value % 1 !== 0

    const animate = () => {
      const now = Date.now()
      if (now < startTime) {
        requestAnimationFrame(animate)
        return
      }

      const elapsed = now - startTime
      const progress = Math.min(elapsed / duration, 1)

      const eased = 1 - Math.pow(1 - progress, 4)
      const current = eased * value

      setDisplayValue(isDecimal ? Math.round(current * 10) / 10 : Math.round(current))

      if (progress < 1) {
        requestAnimationFrame(animate)
      }
    }

    requestAnimationFrame(animate)
  }, [isInView, value, delay])

  return (
    <span ref={ref}>
      {prefix}{displayValue}{suffix}
    </span>
  )
}

// Lab Card with 3D Tilt Effect
interface LabCardProps {
  lab: {
    title: string
    desc: string
    statValue: number
    statPrefix: string
    statSuffix: string
    statLabel: string
    statContext: string
    percent: number
    gradient: string
  }
  index: number
}

function LabCard({ lab, index }: LabCardProps) {
  const cardRef = useRef<HTMLDivElement>(null)
  const isInView = useInView(cardRef, { once: true, margin: '-80px' })

  const x = useMotionValue(0)
  const y = useMotionValue(0)

  const rotateX = useTransform(y, [-100, 100], [8, -8])
  const rotateY = useTransform(x, [-100, 100], [-8, 8])

  const springConfig = { stiffness: 300, damping: 30 }
  const rotateXSpring = useSpring(rotateX, springConfig)
  const rotateYSpring = useSpring(rotateY, springConfig)

  const handleMouseMove = (e: React.MouseEvent<HTMLDivElement>) => {
    if (!cardRef.current) return
    const rect = cardRef.current.getBoundingClientRect()
    const centerX = rect.left + rect.width / 2
    const centerY = rect.top + rect.height / 2
    x.set(e.clientX - centerX)
    y.set(e.clientY - centerY)
  }

  const handleMouseLeave = () => {
    x.set(0)
    y.set(0)
  }

  const icons: { [key: string]: JSX.Element } = {
    'Technical Lab': (
      <svg viewBox="0 0 48 48" fill="none" className="w-full h-full">
        <motion.circle
          cx="24" cy="24" r="18"
          stroke="currentColor" strokeWidth="1.5"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 1.5, delay: index * 0.15, ease: [0.16, 1, 0.3, 1] }}
        />
        <motion.circle
          cx="24" cy="24" r="10"
          stroke="currentColor" strokeWidth="1" strokeDasharray="2 3"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 1.2, delay: index * 0.15 + 0.3, ease: [0.16, 1, 0.3, 1] }}
        />
        <motion.path
          d="M24 6 L24 12" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.5, delay: index * 0.15 + 0.6 }}
        />
        <motion.path
          d="M24 36 L24 42" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.5, delay: index * 0.15 + 0.7 }}
        />
        <motion.path
          d="M6 24 L12 24" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.5, delay: index * 0.15 + 0.8 }}
        />
        <motion.path
          d="M36 24 L42 24" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.5, delay: index * 0.15 + 0.9 }}
        />
        <motion.circle
          cx="24" cy="24" r="3" fill="currentColor"
          initial={{ scale: 0 }}
          animate={isInView ? { scale: 1 } : { scale: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 1, type: 'spring', stiffness: 200 }}
        />
        <motion.path
          d="M24 24 L32 16" stroke="currentColor" strokeWidth="2" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.6, delay: index * 0.15 + 1.1 }}
        />
      </svg>
    ),
    'Match Lab': (
      <svg viewBox="0 0 48 48" fill="none" className="w-full h-full">
        <motion.rect
          x="6" y="10" width="36" height="28" rx="1"
          stroke="currentColor" strokeWidth="1.5"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 1.2, delay: index * 0.15, ease: [0.16, 1, 0.3, 1] }}
        />
        <motion.line
          x1="24" y1="10" x2="24" y2="38" stroke="currentColor" strokeWidth="1"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.6, delay: index * 0.15 + 0.4 }}
        />
        <motion.line
          x1="6" y1="24" x2="42" y2="24" stroke="currentColor" strokeWidth="1"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.6, delay: index * 0.15 + 0.5 }}
        />
        <motion.circle
          cx="14" cy="16" r="2.5" fill="currentColor" className="opacity-70"
          initial={{ scale: 0 }}
          animate={isInView ? { scale: 1 } : { scale: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.7, type: 'spring' }}
        />
        <motion.circle
          cx="34" cy="32" r="2.5" fill="currentColor" className="opacity-70"
          initial={{ scale: 0 }}
          animate={isInView ? { scale: 1 } : { scale: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.8, type: 'spring' }}
        />
        <motion.path
          d="M14 16 Q20 20 24 24 Q28 28 34 32"
          stroke="currentColor" strokeWidth="1.5" strokeDasharray="4 2" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 1, delay: index * 0.15 + 0.9 }}
        />
      </svg>
    ),
    'Strength & Movement': (
      <svg viewBox="0 0 48 48" fill="none" className="w-full h-full">
        <motion.path
          d="M8 36 L16 18 L24 28 L32 10 L40 22"
          stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 1.5, delay: index * 0.15, ease: [0.16, 1, 0.3, 1] }}
        />
        <motion.circle
          cx="16" cy="18" r="3" fill="currentColor" className="opacity-70"
          initial={{ scale: 0 }}
          animate={isInView ? { scale: 1 } : { scale: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.5, type: 'spring' }}
        />
        <motion.circle
          cx="32" cy="10" r="3" fill="currentColor"
          initial={{ scale: 0 }}
          animate={isInView ? { scale: 1 } : { scale: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.7, type: 'spring' }}
        />
        <motion.path
          d="M6 40 L42 40" stroke="currentColor" strokeWidth="1" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.8, delay: index * 0.15 + 0.9 }}
        />
        <motion.path
          d="M32 10 L36 6 M32 10 L36 14" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.5, delay: index * 0.15 + 1.1 }}
        />
      </svg>
    ),
    'Mind & Lifestyle': (
      <svg viewBox="0 0 48 48" fill="none" className="w-full h-full">
        <motion.rect
          x="12" y="8" width="24" height="32" rx="2"
          stroke="currentColor" strokeWidth="1.5"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 1.2, delay: index * 0.15, ease: [0.16, 1, 0.3, 1] }}
        />
        <motion.path
          d="M16 16 L32 16" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.5 }}
        />
        <motion.path
          d="M16 22 L32 22" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.6 }}
        />
        <motion.path
          d="M16 28 L28 28" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.7 }}
        />
        <motion.circle
          cx="24" cy="36" r="2" fill="currentColor"
          initial={{ scale: 0 }}
          animate={isInView ? { scale: 1 } : { scale: 0 }}
          transition={{ duration: 0.4, delay: index * 0.15 + 0.9, type: 'spring' }}
        />
        <motion.path
          d="M18 12 Q24 8 30 12" stroke="currentColor" strokeWidth="1.5" fill="none" strokeLinecap="round"
          initial={{ pathLength: 0 }}
          animate={isInView ? { pathLength: 1 } : { pathLength: 0 }}
          transition={{ duration: 0.6, delay: index * 0.15 + 1 }}
        />
      </svg>
    ),
  }

  return (
    <motion.div
      ref={cardRef}
      initial={{ opacity: 0, y: 50, filter: 'blur(10px)' }}
      whileInView={{ opacity: 1, y: 0, filter: 'blur(0px)' }}
      viewport={{ once: true, margin: '-80px' }}
      transition={{ delay: index * 0.15, duration: 0.8, ease: [0.16, 1, 0.3, 1] }}
      style={{
        rotateX: rotateXSpring,
        rotateY: rotateYSpring,
        transformStyle: 'preserve-3d',
        perspective: '1000px',
      }}
      onMouseMove={handleMouseMove}
      onMouseLeave={handleMouseLeave}
      className="group relative bg-white overflow-hidden cursor-pointer"
    >
      <div
        className={`absolute inset-0 bg-gradient-to-br ${lab.gradient} opacity-0 group-hover:opacity-100 transition-opacity duration-700`}
      />

      <div
        className="relative h-full"
        style={{
          border: '1px solid rgba(0, 0, 0, 0.06)',
          padding: '56px 44px',
          transition: 'all 500ms cubic-bezier(0.16, 1, 0.3, 1)',
          transformStyle: 'preserve-3d',
        }}
      >
        <motion.div
          className="absolute top-0 left-0 w-full h-[2px] origin-left"
          style={{ background: 'linear-gradient(90deg, #F26522, #FF8C4D)' }}
          initial={{ scaleX: 0 }}
          whileHover={{ scaleX: 1 }}
          transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
        />

        <motion.div
          className="w-14 h-14 mb-7 text-[#111111] group-hover:text-[#F26522] transition-colors duration-500"
          style={{ transform: 'translateZ(30px)' }}
          whileHover={{ scale: 1.1 }}
          transition={{ type: 'spring', stiffness: 300 }}
        >
          {icons[lab.title]}
        </motion.div>

        <h4
          className="uppercase mb-4"
          style={{
            fontFamily: "'Manrope', sans-serif",
            fontSize: '13px',
            fontWeight: 600,
            letterSpacing: '0.1em',
            color: '#111111',
            transform: 'translateZ(20px)',
          }}
        >
          {lab.title}
        </h4>

        <p
          style={{
            fontSize: '15px',
            lineHeight: 1.8,
            color: 'rgba(0, 0, 0, 0.7)',
            marginBottom: '40px',
            fontWeight: 400,
            transform: 'translateZ(10px)',
          }}
        >
          {lab.desc}
        </p>

        <div className="pt-7 border-t border-black/6" style={{ transform: 'translateZ(15px)' }}>
          <div
            style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: '44px',
              fontWeight: 600,
              color: '#111111',
              letterSpacing: '-0.02em',
              marginBottom: '8px',
              fontFeatureSettings: "'tnum' 1",
            }}
          >
            <AnimatedCounter
              value={lab.statValue}
              prefix={lab.statPrefix}
              suffix={lab.statSuffix}
              delay={index * 0.15 + 0.5}
            />
          </div>
          <div
            className="uppercase mb-1"
            style={{
              fontSize: '11px',
              fontWeight: 600,
              letterSpacing: '0.1em',
              color: 'rgba(0, 0, 0, 0.5)'
            }}
          >
            {lab.statLabel}
          </div>
          <div style={{ fontSize: '12px', color: 'rgba(0, 0, 0, 0.35)', marginBottom: '14px', fontWeight: 400 }}>
            {lab.statContext}
          </div>

          <div className="relative h-1.5 bg-black/5 rounded-full overflow-hidden">
            <motion.div
              initial={{ width: 0 }}
              whileInView={{ width: `${lab.percent}%` }}
              viewport={{ once: true }}
              transition={{ duration: 1.8, delay: index * 0.15 + 0.8, ease: [0.16, 1, 0.3, 1] }}
              className="absolute h-full rounded-full"
              style={{
                background: 'linear-gradient(90deg, #F26522 0%, #FF8C4D 100%)',
                boxShadow: '0 0 12px rgba(242, 101, 34, 0.4)',
              }}
            />
          </div>
        </div>

        <div
          className="absolute -bottom-20 -right-20 w-40 h-40 rounded-full opacity-0 group-hover:opacity-20 transition-opacity duration-700 blur-3xl"
          style={{ background: '#F26522' }}
        />
      </div>
    </motion.div>
  )
}

// Rhythm Card Component for Daily Schedule
interface RhythmCardProps {
  block: {
    time: string
    hours: number
    title: string
    details: string[]
    color: string
    icon: JSX.Element
  }
  index: number
}

function RhythmCard({ block, index }: RhythmCardProps) {
  const cardRef = useRef<HTMLDivElement>(null)
  const isInView = useInView(cardRef, { once: true, margin: '-50px' })
  const [isHovered, setIsHovered] = useState(false)

  const circumference = 2 * Math.PI * 42
  const progressPercent = block.hours > 0 ? (block.hours / 4) * 100 : 25
  const strokeDashoffset = circumference - (circumference * progressPercent) / 100

  return (
    <motion.div
      ref={cardRef}
      initial={{ opacity: 0, y: 40, filter: 'blur(8px)' }}
      whileInView={{ opacity: 1, y: 0, filter: 'blur(0px)' }}
      viewport={{ once: true }}
      transition={{ duration: 0.8, delay: index * 0.12, ease: [0.16, 1, 0.3, 1] }}
      className="group relative"
      onMouseEnter={() => setIsHovered(true)}
      onMouseLeave={() => setIsHovered(false)}
    >
      <motion.div
        className="relative p-8 md:p-10 rounded-2xl overflow-hidden"
        style={{
          background: 'linear-gradient(135deg, rgba(255,255,255,0.9) 0%, rgba(255,255,255,0.7) 100%)',
          boxShadow: isHovered
            ? `0 25px 50px -12px ${block.color}20, 0 0 0 1px ${block.color}20`
            : '0 8px 32px rgba(0, 0, 0, 0.04), 0 0 0 1px rgba(0, 0, 0, 0.02)',
          transition: 'all 0.5s cubic-bezier(0.16, 1, 0.3, 1)',
          transform: isHovered ? 'translateY(-4px)' : 'translateY(0)',
        }}
      >
        <div className="relative w-24 h-24 mx-auto mb-8">
          <svg className="w-full h-full -rotate-90" viewBox="0 0 96 96">
            <circle
              cx="48"
              cy="48"
              r="42"
              stroke="rgba(0,0,0,0.04)"
              strokeWidth="2"
              fill="none"
            />
            <motion.circle
              cx="48"
              cy="48"
              r="42"
              stroke={block.color}
              strokeWidth="3"
              fill="none"
              strokeLinecap="round"
              strokeDasharray={circumference}
              initial={{ strokeDashoffset: circumference }}
              animate={isInView ? { strokeDashoffset: strokeDashoffset } : { strokeDashoffset: circumference }}
              transition={{ duration: 1.5, delay: index * 0.15 + 0.3, ease: [0.16, 1, 0.3, 1] }}
            />
            {[0, 90, 180, 270].map((angle, i) => (
              <motion.line
                key={i}
                x1="48"
                y1="8"
                x2="48"
                y2="14"
                stroke="rgba(0,0,0,0.15)"
                strokeWidth="1"
                transform={`rotate(${angle} 48 48)`}
                initial={{ opacity: 0 }}
                animate={isInView ? { opacity: 1 } : { opacity: 0 }}
                transition={{ delay: index * 0.15 + 0.5 + i * 0.1 }}
              />
            ))}
          </svg>

          <motion.div
            className="absolute inset-0 flex flex-col items-center justify-center"
            initial={{ scale: 0, opacity: 0 }}
            animate={isInView ? { scale: 1, opacity: 1 } : { scale: 0, opacity: 0 }}
            transition={{ duration: 0.5, delay: index * 0.15 + 0.6, type: 'spring', stiffness: 150 }}
          >
            {block.hours > 0 ? (
              <>
                <span style={{ fontSize: '28px', fontWeight: 600, color: block.color, lineHeight: 1 }}>
                  {block.hours}
                </span>
                <span style={{ fontSize: '10px', fontWeight: 500, color: 'rgba(0,0,0,0.4)', textTransform: 'uppercase', letterSpacing: '0.1em' }}>
                  hours
                </span>
              </>
            ) : (
              <div style={{ color: block.color }}>
                {block.icon}
              </div>
            )}
          </motion.div>
        </div>

        <div className="text-center mb-6">
          <span
            style={{
              fontSize: '13px',
              fontWeight: 600,
              color: '#111111',
              letterSpacing: '0.02em',
            }}
          >
            {block.time}
          </span>
        </div>

        <h4
          className="text-center uppercase mb-6"
          style={{
            fontFamily: "'Manrope', sans-serif",
            fontSize: '12px',
            fontWeight: 600,
            letterSpacing: '0.12em',
            color: block.color,
          }}
        >
          {block.title}
        </h4>

        <div className="space-y-3">
          {block.details.map((detail, i) => (
            <motion.div
              key={i}
              className="flex items-start gap-3"
              initial={{ opacity: 0, x: -10 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.15 + 0.8 + i * 0.1 }}
            >
              <motion.div
                className="w-1.5 h-1.5 rounded-full mt-2 flex-shrink-0"
                style={{ background: block.color }}
                whileHover={{ scale: 1.5 }}
              />
              <span style={{ fontSize: '14px', lineHeight: 1.6, color: 'rgba(0, 0, 0, 0.7)', fontWeight: 400 }}>
                {detail}
              </span>
            </motion.div>
          ))}
        </div>

        <motion.div
          className="absolute bottom-0 left-0 w-full h-1"
          style={{ background: `linear-gradient(90deg, ${block.color}, transparent)` }}
          initial={{ scaleX: 0 }}
          animate={{ scaleX: isHovered ? 1 : 0 }}
          transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
        />
      </motion.div>
    </motion.div>
  )
}

// Milestone Card Component for Long Arc Journey
interface MilestoneCardProps {
  milestone: {
    year: number | string
    phase: string
    timeframe: string
    desc: string
    position: { left: string, top: string }
    mobileOrder: number
  }
  index: number
}

function MilestoneCard({ milestone, index }: MilestoneCardProps) {
  const cardRef = useRef<HTMLDivElement>(null)
  const isInView = useInView(cardRef, { once: true, margin: '-50px' })
  const [isHovered, setIsHovered] = useState(false)

  const x = useMotionValue(0)
  const y = useMotionValue(0)

  const rotateX = useTransform(y, [-50, 50], [5, -5])
  const rotateY = useTransform(x, [-50, 50], [-5, 5])

  const springConfig = { stiffness: 300, damping: 30 }
  const rotateXSpring = useSpring(rotateX, springConfig)
  const rotateYSpring = useSpring(rotateY, springConfig)

  const handleMouseMove = (e: React.MouseEvent<HTMLDivElement>) => {
    if (!cardRef.current) return
    const rect = cardRef.current.getBoundingClientRect()
    const centerX = rect.left + rect.width / 2
    const centerY = rect.top + rect.height / 2
    x.set(e.clientX - centerX)
    y.set(e.clientY - centerY)
  }

  const handleMouseLeave = () => {
    x.set(0)
    y.set(0)
    setIsHovered(false)
  }

  return (
    <motion.div
      ref={cardRef}
      className="absolute md:max-w-[260px] md:block hidden"
      style={{
        left: milestone.position.left,
        top: milestone.position.top,
        perspective: '1000px',
      }}
      initial={{ opacity: 0, y: 30, filter: 'blur(8px)' }}
      whileInView={{ opacity: 1, y: 0, filter: 'blur(0px)' }}
      viewport={{ once: true }}
      transition={{ duration: 0.8, delay: 0.6 + index * 0.2, ease: [0.16, 1, 0.3, 1] }}
      onMouseMove={handleMouseMove}
      onMouseEnter={() => setIsHovered(true)}
      onMouseLeave={handleMouseLeave}
    >
      <motion.div
        className="relative p-6 rounded-xl"
        style={{
          background: isHovered
            ? 'linear-gradient(135deg, rgba(255,255,255,1) 0%, rgba(250,250,250,1) 100%)'
            : 'linear-gradient(135deg, rgba(255,255,255,0.9) 0%, rgba(255,255,255,0.6) 100%)',
          backdropFilter: 'blur(10px)',
          boxShadow: isHovered
            ? '0 20px 40px rgba(0, 0, 0, 0.08), 0 0 0 1px rgba(0, 0, 0, 0.05)'
            : '0 8px 24px rgba(0, 0, 0, 0.04), 0 0 0 1px rgba(0, 0, 0, 0.03)',
          rotateX: rotateXSpring,
          rotateY: rotateYSpring,
          transformStyle: 'preserve-3d',
          transition: 'box-shadow 0.4s ease, background 0.4s ease',
        }}
      >
        <motion.div
          style={{
            fontFamily: "'Manrope', sans-serif",
            fontSize: '36px',
            fontWeight: 600,
            color: '#111111',
            letterSpacing: '-0.02em',
            marginBottom: '4px',
            transform: 'translateZ(20px)',
          }}
        >
          {milestone.year}
        </motion.div>

        <h3
          style={{
            fontFamily: "'Manrope', sans-serif",
            fontSize: '18px',
            fontWeight: 500,
            color: '#111111',
            marginBottom: '6px',
            transform: 'translateZ(15px)',
          }}
        >
          {milestone.phase}
        </h3>

        <div
          className="inline-block mb-4"
          style={{
            fontSize: '10px',
            fontWeight: 600,
            letterSpacing: '0.12em',
            textTransform: 'uppercase',
            color: 'rgba(0, 0, 0, 0.4)',
            background: 'rgba(0, 0, 0, 0.03)',
            padding: '4px 10px',
            borderRadius: '100px',
            transform: 'translateZ(10px)',
          }}
        >
          {milestone.timeframe}
        </div>

        <p
          style={{
            fontSize: '14px',
            lineHeight: 1.7,
            color: 'rgba(0, 0, 0, 0.65)',
            fontWeight: 400,
            transform: 'translateZ(5px)',
          }}
        >
          {milestone.desc}
        </p>

        <motion.div
          className="absolute w-3 h-3 rounded-full"
          style={{
            background: '#111111',
            left: '-24px',
            top: '24px',
            boxShadow: isHovered ? '0 0 16px rgba(0, 0, 0, 0.2)' : '0 0 8px rgba(0, 0, 0, 0.15)',
            transition: 'box-shadow 0.3s ease',
          }}
          animate={isInView ? { scale: [0, 1.2, 1] } : { scale: 0 }}
          transition={{ delay: 0.7 + index * 0.2, duration: 0.4 }}
        />
      </motion.div>
    </motion.div>
  )
}

// Simple counter for totals
function AnimatedCounterSimple({ target, duration = 2, delay = 0 }: { target: number, duration?: number, delay?: number }) {
  const ref = useRef(null)
  const isInView = useInView(ref, { once: true, margin: '-50px' })
  const [displayValue, setDisplayValue] = useState(0)

  useEffect(() => {
    if (!isInView) return

    const durationMs = duration * 1000
    const startTime = Date.now() + delay * 1000

    const animate = () => {
      const now = Date.now()
      if (now < startTime) {
        requestAnimationFrame(animate)
        return
      }

      const elapsed = now - startTime
      const progress = Math.min(elapsed / durationMs, 1)
      const eased = 1 - Math.pow(1 - progress, 4)

      setDisplayValue(Math.round(eased * target))

      if (progress < 1) {
        requestAnimationFrame(animate)
      }
    }

    requestAnimationFrame(animate)
  }, [isInView, target, duration, delay])

  return (
    <span ref={ref} style={{ fontSize: '32px', fontWeight: 600, color: '#111111', letterSpacing: '-0.02em' }}>
      {displayValue}
    </span>
  )
}

const facilityImages = [
  { src: '/photos/LBCOURTSETTING.jpg', caption: 'The Environment — Laguna Beach', alt: 'Court setting and environment' },
  { src: '/photos/VideoAnalysisRoom.png', caption: 'Video Analysis Room — Tactical Review', alt: 'Video analysis and tactical review room' },
  { src: '/photos/IMG_3777 (1).JPG', caption: '7:32 AM — South Court 1', alt: 'Tennis court close-up training session' },
  { src: '/photos/GymSetting.png', caption: 'Strength Lab — Performance Training', alt: 'Gym and fitness facility' },
  { src: '/photos/playerhousing.png', caption: 'Athlete Housing — Ocean View Study', alt: 'Player housing with ocean view study area' },
]

export default function VYLOPage() {
  const [scrollHintVisible, setScrollHintVisible] = useState(true)
  const [heroTextVisible, setHeroTextVisible] = useState(false)
  const [carouselIndex, setCarouselIndex] = useState(0)
  const [activeTimeline, setActiveTimeline] = useState<number | null>(null)

  const { scrollY } = useScroll()
  const heroScale = useTransform(scrollY, [0, 1000], [1, 1.15])

  useEffect(() => {
    const scrollTimer = setTimeout(() => setScrollHintVisible(false), 8000)
    const heroTimer = setTimeout(() => setHeroTextVisible(true), 4000)

    const handleScroll = () => {
      if (window.scrollY > 100) setScrollHintVisible(false)
    }
    window.addEventListener('scroll', handleScroll)

    return () => {
      clearTimeout(scrollTimer)
      clearTimeout(heroTimer)
      window.removeEventListener('scroll', handleScroll)
    }
  }, [])

  const nextSlide = () => setCarouselIndex((prev) => (prev + 1) % facilityImages.length)
  const prevSlide = () => setCarouselIndex((prev) => (prev - 1 + facilityImages.length) % facilityImages.length)

  return (
    <div
      className="bg-white"
      style={{
        fontFamily: "'Manrope', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
        fontFeatureSettings: "'cv05' 1, 'cv08' 1",
        WebkitFontSmoothing: 'antialiased',
        MozOsxFontSmoothing: 'grayscale',
      }}
    >
      {/* HEADER */}
      <header className="fixed top-0 left-0 right-0 z-50">
        <div className="max-w-[1440px] mx-auto px-8 md:px-12 py-8 flex items-center justify-between">
          <Link href="/vylo" className="hover:opacity-80 transition-opacity">
            <Image src="/logos/vylo.png" alt="VYLO" width={220} height={73} className="h-[70px] w-auto" priority />
          </Link>
        </div>
      </header>

      {/* HERO SECTION */}
      <section className="relative w-full min-h-screen bg-[#0A0A0A]">
        <div className="grid grid-cols-1 lg:grid-cols-12 min-h-screen">
          <div className="lg:col-span-5 flex flex-col justify-center px-8 md:px-16 lg:px-20 py-32 lg:py-20">
            <motion.div
              initial={{ opacity: 0, x: -30 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 1.4, ease: [0.16, 1, 0.3, 1] }}
            >
              <div style={{
                fontFamily: "'Manrope', sans-serif",
                fontSize: '11px',
                fontWeight: 500,
                letterSpacing: '0.15em',
                textTransform: 'uppercase',
                color: 'rgba(255, 255, 255, 0.5)',
                marginBottom: '24px',
              }}>
                In partnership with Laguna Beach Tennis Academy
              </div>

              <h1
                style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: 'clamp(42px, 5vw, 72px)',
                  fontWeight: 600,
                  lineHeight: 1.1,
                  color: '#FFFFFF',
                  marginBottom: '40px',
                  letterSpacing: '-0.02em',
                  textTransform: 'uppercase',
                }}
              >
                Professional tour coaching.
                <br />
                Full-time development.
                <br />
                Ten families annually.
              </h1>

              <div style={{
                width: '60px',
                height: '1px',
                background: '#F26522',
                marginBottom: '32px',
              }} />

              <p
                style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: 'clamp(15px, 1.6vw, 17px)',
                  fontWeight: 400,
                  color: 'rgba(255, 255, 255, 0.75)',
                  maxWidth: '480px',
                  lineHeight: 1.7,
                  letterSpacing: '0.01em',
                  marginBottom: '56px',
                }}
              >
                Ten athletes. Direct coaching from ATP/WTA-credentialed professionals.
                Quarterly documentation you can read and share with college coaches.
              </p>

              <motion.div
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.6, duration: 1 }}
              >
                <Link
                  href="/book"
                  className="group inline-flex items-center gap-4 px-8 py-4 bg-[#F26522] hover:bg-[#E85A10] transition-all duration-300"
                  style={{
                    fontFamily: "'Manrope', sans-serif",
                    fontSize: '13px',
                    fontWeight: 600,
                    letterSpacing: '0.08em',
                    textTransform: 'uppercase',
                    color: '#FFFFFF',
                  }}
                >
                  Apply Now
                  <svg width="16" height="16" viewBox="0 0 16 16" fill="none" className="group-hover:translate-x-1 transition-transform">
                    <path d="M1 8H15M15 8L8 1M15 8L8 15" stroke="white" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
                  </svg>
                </Link>
                <p style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: '12px',
                  color: 'rgba(255, 255, 255, 0.4)',
                  marginTop: '16px',
                  fontWeight: 400,
                }}>
                  January 2026 program
                </p>
              </motion.div>
            </motion.div>
          </div>

          <div className="lg:col-span-7 relative min-h-[60vh] lg:min-h-screen">
            <motion.div
              className="absolute inset-0"
              initial={{ opacity: 0, scale: 1.05 }}
              animate={{ opacity: 1, scale: 1 }}
              transition={{ duration: 1.6, ease: [0.16, 1, 0.3, 1] }}
            >
              <img
                src="/photos/KarueFH2.jpg"
                alt="Elite tennis training at VYLO Laguna Beach"
                className="w-full h-full object-cover"
                style={{
                  filter: 'saturate(0.9) contrast(1.05)',
                }}
              />
              <div className="absolute inset-0 bg-gradient-to-l from-transparent via-transparent to-[#0A0A0A]/20 lg:to-[#0A0A0A]/60" />
            </motion.div>
          </div>
        </div>

        <AnimatePresence>
          {scrollHintVisible && (
            <motion.div
              className="absolute bottom-12 left-1/2 transform -translate-x-1/2 z-20"
              initial={{ opacity: 0, y: -10 }}
              animate={{ opacity: 1, y: 0 }}
              exit={{ opacity: 0, y: 10 }}
              transition={{ duration: 0.5 }}
            >
              <motion.div
                animate={{ y: [0, 8, 0] }}
                transition={{ duration: 2, repeat: Infinity, ease: 'easeInOut' }}
              >
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="white" strokeWidth="1.5">
                  <path d="M12 5v14M19 12l-7 7-7-7" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              </motion.div>
            </motion.div>
          )}
        </AnimatePresence>
      </section>

      {/* OPENING STATEMENT */}
      <section style={{ padding: '160px 0', background: '#FAFAF9' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, margin: '-100px' }}
            transition={{ duration: 1, ease: [0.16, 1, 0.3, 1] }}
            style={{ maxWidth: '900px', margin: '0 auto' }}
          >
            <p style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(24px, 3vw, 36px)',
              fontWeight: 500,
              lineHeight: 1.6,
              color: '#111111',
              textAlign: 'center',
              letterSpacing: '-0.01em',
            }}>
              Professional training creates professional results. Ten athletes receive direct coaching
              from ATP and WTA tour professionals. Every three months, families receive a comprehensive
              quarterly report documenting technical development, match performance, and measurable progress.
            </p>
          </motion.div>
        </div>
      </section>

      {/* THE FOUR LABS */}
      <section style={{ padding: '180px 0 160px', background: '#FFFFFF' }}>
        <div className="max-w-[1440px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-24 max-w-[700px]"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8, ease: [0.16, 1, 0.3, 1] }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
              lineHeight: 1.15,
            }}>
              Four Training Systems
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              Technical precision. Match intelligence. Physical development. Mental performance.
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
            <LabCard
              lab={{
                title: 'Technical Lab',
                desc: 'ATP/WTA tour professionals analyze and refine stroke mechanics through video comparison and biomechanical assessment.',
                statValue: 127,
                statPrefix: '',
                statSuffix: '',
                statLabel: 'Technical Adjustments',
                statContext: 'Average per athlete annually',
                percent: 89,
                gradient: 'from-orange-50 to-orange-100',
              }}
              index={0}
            />
            <LabCard
              lab={{
                title: 'Match Lab',
                desc: 'Pattern recognition, situational training, and competitive strategy development based on opponent analysis.',
                statValue: 94,
                statPrefix: '',
                statSuffix: '',
                statLabel: 'Match Simulations',
                statContext: 'Per athlete per quarter',
                percent: 76,
                gradient: 'from-blue-50 to-blue-100',
              }}
              index={1}
            />
            <LabCard
              lab={{
                title: 'Strength & Movement',
                desc: 'Sport-specific conditioning protocols designed by certified strength coaches with tour-level experience.',
                statValue: 18,
                statPrefix: '',
                statSuffix: '%',
                statLabel: 'Power Output Increase',
                statContext: 'Average first-year improvement',
                percent: 82,
                gradient: 'from-green-50 to-green-100',
              }}
              index={2}
            />
            <LabCard
              lab={{
                title: 'Mind & Lifestyle',
                desc: 'Performance psychology, nutrition planning, recovery protocols, and academic coordination.',
                statValue: 3.8,
                statPrefix: '',
                statSuffix: '',
                statLabel: 'GPA Average',
                statContext: 'Across all VYLO athletes',
                percent: 92,
                gradient: 'from-purple-50 to-purple-100',
              }}
              index={3}
            />
          </div>
        </div>
      </section>

      {/* DAILY RHYTHM */}
      <section style={{ padding: '160px 0', background: '#FAFAF9' }}>
        <div className="max-w-[1440px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20 max-w-[800px] mx-auto text-center"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Training Architecture
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              Structured daily training blocks designed for progressive skill development
              and measurable performance gains.
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-16">
            <RhythmCard
              block={{
                time: '6:30 – 9:00 AM',
                hours: 2.5,
                title: 'Technical Training',
                details: [
                  'Stroke mechanics refinement',
                  'Video analysis review',
                  'Pattern development drills',
                  'Movement efficiency work',
                ],
                color: '#F26522',
                icon: <div></div>,
              }}
              index={0}
            />
            <RhythmCard
              block={{
                time: '9:00 – 12:00 PM',
                hours: 3,
                title: 'Academic Block',
                details: [
                  'College-prep curriculum',
                  'Individual tutoring',
                  'Study hall sessions',
                  'Academic coordination',
                ],
                color: '#3B82F6',
                icon: <div></div>,
              }}
              index={1}
            />
            <RhythmCard
              block={{
                time: '1:00 – 3:30 PM',
                hours: 2.5,
                title: 'Strength & Conditioning',
                details: [
                  'Performance training',
                  'Movement assessment',
                  'Injury prevention protocols',
                  'Recovery techniques',
                ],
                color: '#10B981',
                icon: <div></div>,
              }}
              index={2}
            />
            <RhythmCard
              block={{
                time: '4:00 – 7:00 PM',
                hours: 3,
                title: 'Match Simulation',
                details: [
                  'Competitive point play',
                  'Tactical scenario training',
                  'Mental performance work',
                  'Match strategy implementation',
                ],
                color: '#8B5CF6',
                icon: <div></div>,
              }}
              index={3}
            />
          </div>

          <motion.div
            className="text-center pt-12 border-t border-black/10"
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            viewport={{ once: true }}
            transition={{ delay: 0.6, duration: 0.8 }}
          >
            <div className="flex items-center justify-center gap-3 mb-3">
              <AnimatedCounterSimple target={32} delay={0.2} />
              <span style={{ fontSize: '16px', color: 'rgba(0, 0, 0, 0.5)', fontWeight: 500 }}>
                hours per week
              </span>
            </div>
            <p style={{ fontSize: '14px', color: 'rgba(0, 0, 0, 0.4)', fontWeight: 400 }}>
              Structured training time — Monday through Saturday
            </p>
          </motion.div>
        </div>
      </section>

      {/* SELECTION CRITERIA */}
      <section style={{ padding: '160px 0', background: '#FFFFFF' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20 max-w-[700px]"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Selection Criteria
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              Ten positions. Evaluated on competitive ranking, technical foundation,
              trainability, and commitment level.
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-12">
            {[
              {
                number: '01',
                title: 'Competitive Standing',
                desc: 'Current UTR 9.0 or higher. National or sectional tournament experience. Demonstrable competitive trajectory over previous 12 months.',
              },
              {
                number: '02',
                title: 'Technical Foundation',
                desc: 'Developed stroke mechanics across all shot categories. Ability to generate pace and spin. Footwork patterns appropriate for competitive play.',
              },
              {
                number: '03',
                title: 'Physical Readiness',
                desc: 'Age 14-18. Capacity for sustained high-intensity training. No significant injury history requiring program modification.',
              },
              {
                number: '04',
                title: 'Program Commitment',
                desc: 'Full-time enrollment (minimum 9 months). Family understanding of time investment and travel requirements. Academic eligibility maintained.',
              },
            ].map((criterion, i) => (
              <motion.div
                key={i}
                className="relative"
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.15, duration: 0.8, ease: [0.16, 1, 0.3, 1] }}
              >
                <div style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: '14px',
                  fontWeight: 600,
                  letterSpacing: '0.12em',
                  color: 'rgba(0, 0, 0, 0.3)',
                  marginBottom: '16px',
                }}>
                  {criterion.number}
                </div>
                <h3 style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: '24px',
                  fontWeight: 600,
                  color: '#111111',
                  marginBottom: '12px',
                  letterSpacing: '-0.01em',
                }}>
                  {criterion.title}
                </h3>
                <p style={{
                  fontSize: '16px',
                  lineHeight: 1.7,
                  color: 'rgba(0, 0, 0, 0.6)',
                  fontWeight: 400,
                }}>
                  {criterion.desc}
                </p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* COHORT PROFILE */}
      <section style={{ padding: '160px 0', background: '#FAFAF9' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20 max-w-[700px]"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Current Cohort Profile
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              January 2025 enrollment. Ten athletes training full-time.
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {[
              {
                metric: 'UTR 11.2',
                label: 'Average Rating',
                detail: 'Range: 9.4 – 13.1',
              },
              {
                metric: '15.8 years',
                label: 'Average Age',
                detail: 'Range: 14 – 18',
              },
              {
                metric: '7 states',
                label: 'Geographic Diversity',
                detail: 'CA, TX, FL, NY, AZ, IL, WA',
              },
              {
                metric: '6 athletes',
                label: 'Prior Academy Experience',
                detail: 'IMG, USTA, Evert, Saddlebrook',
              },
              {
                metric: '4 athletes',
                label: 'National Rankings',
                detail: 'Top 50 in age group',
              },
              {
                metric: '100%',
                label: 'College Track',
                detail: 'All eligible for NCAA Division I',
              },
            ].map((stat, i) => (
              <motion.div
                key={i}
                className="text-center p-8 bg-white rounded-lg"
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.1, duration: 0.8 }}
                style={{
                  boxShadow: '0 4px 24px rgba(0, 0, 0, 0.04)',
                }}
              >
                <div style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: '36px',
                  fontWeight: 600,
                  color: '#111111',
                  marginBottom: '8px',
                  letterSpacing: '-0.02em',
                }}>
                  {stat.metric}
                </div>
                <div style={{
                  fontSize: '13px',
                  fontWeight: 600,
                  letterSpacing: '0.08em',
                  textTransform: 'uppercase',
                  color: 'rgba(0, 0, 0, 0.5)',
                  marginBottom: '8px',
                }}>
                  {stat.label}
                </div>
                <div style={{
                  fontSize: '14px',
                  color: 'rgba(0, 0, 0, 0.4)',
                  fontWeight: 400,
                }}>
                  {stat.detail}
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* PATHWAY TO D1 & BEYOND */}
      <section style={{ padding: '160px 0', background: '#FFFFFF' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20 max-w-[900px] mx-auto text-center"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Division I Pathway
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              VYLO athletes compete for positions at the nation's top collegiate programs.
              Direct coaching relationships, documented progress reports, and tournament exposure
              create visibility with Division I recruiting coordinators.
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-12 mb-20">
            {[
              {
                stat: '94%',
                label: 'Division I Placement',
                context: '2020-2024 cohorts',
              },
              {
                stat: '12',
                label: 'Top-25 Programs',
                context: 'ITA rankings',
              },
              {
                stat: '$3.2M',
                label: 'Athletic Scholarships',
                context: 'Awarded 2024',
              },
            ].map((item, i) => (
              <motion.div
                key={i}
                className="text-center"
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.15, duration: 0.8 }}
              >
                <div style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: '56px',
                  fontWeight: 600,
                  color: '#111111',
                  marginBottom: '12px',
                  letterSpacing: '-0.02em',
                }}>
                  {item.stat}
                </div>
                <div style={{
                  fontSize: '16px',
                  fontWeight: 600,
                  color: '#111111',
                  marginBottom: '6px',
                  letterSpacing: '-0.01em',
                }}>
                  {item.label}
                </div>
                <div style={{
                  fontSize: '14px',
                  color: 'rgba(0, 0, 0, 0.4)',
                  fontWeight: 400,
                }}>
                  {item.context}
                </div>
              </motion.div>
            ))}
          </div>

          <motion.div
            className="p-12 bg-[#FAFAF9] rounded-lg"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h3 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: '28px',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '20px',
              letterSpacing: '-0.01em',
            }}>
              Recent Commitments
            </h3>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              {[
                { name: 'Emma Rodriguez', school: 'Stanford University', year: '2024' },
                { name: 'James Park', school: 'Duke University', year: '2024' },
                { name: 'Sofia Andreev', school: 'USC', year: '2024' },
                { name: 'Marcus Chen', school: 'Northwestern University', year: '2025' },
                { name: 'Isabella Santos', school: 'University of Virginia', year: '2025' },
                { name: 'Noah Williams', school: 'Wake Forest University', year: '2025' },
              ].map((commit, i) => (
                <div key={i} style={{
                  padding: '16px',
                  borderLeft: '2px solid #111111',
                }}>
                  <div style={{
                    fontSize: '16px',
                    fontWeight: 600,
                    color: '#111111',
                    marginBottom: '4px',
                  }}>
                    {commit.name}
                  </div>
                  <div style={{
                    fontSize: '14px',
                    color: 'rgba(0, 0, 0, 0.5)',
                    fontWeight: 400,
                  }}>
                    {commit.school} — Class of {commit.year}
                  </div>
                </div>
              ))}
            </div>
          </motion.div>
        </div>
      </section>

      {/* BEYOND D1 */}
      <section style={{ padding: '160px 0', background: '#0A0A0A' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            className="max-w-[900px] mx-auto"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 1, ease: [0.16, 1, 0.3, 1] }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#FFFFFF',
              marginBottom: '32px',
              letterSpacing: '-0.02em',
            }}>
              Beyond Division I
            </h2>
            <p style={{
              fontSize: '19px',
              lineHeight: 1.8,
              color: 'rgba(255, 255, 255, 0.75)',
              marginBottom: '28px',
              fontWeight: 400,
            }}>
              College tennis provides competition, education, and structure. Professional tennis
              requires different preparation. VYLO trains athletes for both pathways.
            </p>
            <p style={{
              fontSize: '19px',
              lineHeight: 1.8,
              color: 'rgba(255, 255, 255, 0.75)',
              marginBottom: '28px',
              fontWeight: 400,
            }}>
              Three athletes from the 2020-2023 cohorts currently compete on the ATP or WTA tours.
              Two others compete professionally on the ITF circuit. The training architecture
              remains identical: technical precision, match intelligence, physical development,
              mental performance.
            </p>
            <p style={{
              fontSize: '19px',
              lineHeight: 1.8,
              color: 'rgba(255, 255, 255, 0.75)',
              fontWeight: 400,
            }}>
              Families determine the appropriate endpoint. VYLO provides the professional-level
              preparation that makes either destination achievable.
            </p>
          </motion.div>

          <motion.div
            className="grid grid-cols-1 md:grid-cols-2 gap-12 mt-20"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ delay: 0.3, duration: 0.8 }}
          >
            <div className="p-10 border border-white/10 rounded-lg">
              <div style={{
                fontSize: '13px',
                fontWeight: 600,
                letterSpacing: '0.08em',
                textTransform: 'uppercase',
                color: 'rgba(255, 255, 255, 0.5)',
                marginBottom: '16px',
              }}>
                Professional Tour Athletes
              </div>
              <div style={{
                fontSize: '48px',
                fontWeight: 600,
                color: '#FFFFFF',
                marginBottom: '12px',
                letterSpacing: '-0.02em',
              }}>
                5
              </div>
              <p style={{
                fontSize: '15px',
                color: 'rgba(255, 255, 255, 0.6)',
                fontWeight: 400,
                lineHeight: 1.6,
              }}>
                Former VYLO athletes currently competing on ATP, WTA, or ITF professional circuits
              </p>
            </div>
            <div className="p-10 border border-white/10 rounded-lg">
              <div style={{
                fontSize: '13px',
                fontWeight: 600,
                letterSpacing: '0.08em',
                textTransform: 'uppercase',
                color: 'rgba(255, 255, 255, 0.5)',
                marginBottom: '16px',
              }}>
                Highest ATP Ranking
              </div>
              <div style={{
                fontSize: '48px',
                fontWeight: 600,
                color: '#FFFFFF',
                marginBottom: '12px',
                letterSpacing: '-0.02em',
              }}>
                No. 287
              </div>
              <p style={{
                fontSize: '15px',
                color: 'rgba(255, 255, 255, 0.6)',
                fontWeight: 400,
                lineHeight: 1.6,
              }}>
                Achieved by 2021 cohort graduate, age 19
              </p>
            </div>
          </motion.div>
        </div>
      </section>

      {/* QUARTERLY REPORT PREVIEW */}
      <section style={{ padding: '160px 0', background: '#FFFFFF' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20 max-w-[800px]"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Quarterly Progress Documentation
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              Every 90 days, families receive comprehensive performance documentation.
              Technical analysis, match statistics, physical benchmarks, and development
              trajectory—formatted for college recruiting coordinators and family review.
            </p>
          </motion.div>

          <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">
            <motion.div
              initial={{ opacity: 0, x: -30 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.8 }}
            >
              <div className="bg-[#FAFAF9] p-12 rounded-lg">
                <div style={{
                  fontSize: '13px',
                  fontWeight: 600,
                  letterSpacing: '0.08em',
                  textTransform: 'uppercase',
                  color: 'rgba(0, 0, 0, 0.4)',
                  marginBottom: '24px',
                }}>
                  Report Contents
                </div>

                {[
                  {
                    title: 'Video Comparison Analysis',
                    desc: 'Side-by-side footage showing technical refinements across serve, groundstrokes, and movement patterns',
                  },
                  {
                    title: 'Match Performance Metrics',
                    desc: 'Win-loss record, UTR progression, opponent quality analysis, and situational statistics',
                  },
                  {
                    title: 'Physical Benchmark Data',
                    desc: 'Strength gains, speed measurements, movement efficiency scores, and injury prevention notes',
                  },
                  {
                    title: 'Technical Development Summary',
                    desc: 'Stroke mechanics assessment, pattern implementation progress, and next-quarter focus areas',
                  },
                  {
                    title: 'Coach Evaluation',
                    desc: 'Written assessment from lead coach detailing competitive readiness and development trajectory',
                  },
                ].map((section, i) => (
                  <div key={i} style={{
                    marginBottom: i < 4 ? '28px' : '0',
                    paddingBottom: i < 4 ? '28px' : '0',
                    borderBottom: i < 4 ? '1px solid rgba(0, 0, 0, 0.08)' : 'none',
                  }}>
                    <h4 style={{
                      fontSize: '16px',
                      fontWeight: 600,
                      color: '#111111',
                      marginBottom: '8px',
                      letterSpacing: '-0.01em',
                    }}>
                      {section.title}
                    </h4>
                    <p style={{
                      fontSize: '14px',
                      lineHeight: 1.7,
                      color: 'rgba(0, 0, 0, 0.5)',
                      fontWeight: 400,
                    }}>
                      {section.desc}
                    </p>
                  </div>
                ))}
              </div>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, x: 30 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.8, delay: 0.2 }}
            >
              <div className="relative aspect-[8.5/11] bg-white rounded-lg shadow-2xl overflow-hidden">
                <div className="absolute inset-0 p-8" style={{
                  background: 'linear-gradient(180deg, #FAFAF9 0%, #FFFFFF 100%)',
                }}>
                  <div style={{
                    fontSize: '11px',
                    fontWeight: 600,
                    letterSpacing: '0.12em',
                    textTransform: 'uppercase',
                    color: 'rgba(0, 0, 0, 0.3)',
                    marginBottom: '32px',
                  }}>
                    Q1 2025 Performance Report
                  </div>

                  <div style={{
                    fontSize: '24px',
                    fontWeight: 600,
                    color: '#111111',
                    marginBottom: '8px',
                    letterSpacing: '-0.01em',
                  }}>
                    Athlete Development Summary
                  </div>

                  <div style={{
                    fontSize: '13px',
                    color: 'rgba(0, 0, 0, 0.4)',
                    marginBottom: '32px',
                    fontWeight: 400,
                  }}>
                    January – March 2025
                  </div>

                  <div className="space-y-6">
                    <div className="h-3 bg-black/5 rounded-full w-full"></div>
                    <div className="h-3 bg-black/5 rounded-full w-5/6"></div>
                    <div className="h-3 bg-black/5 rounded-full w-full"></div>
                    <div className="h-3 bg-black/5 rounded-full w-4/5"></div>
                    <div className="h-20 bg-black/5 rounded-lg w-full mt-8"></div>
                    <div className="h-3 bg-black/5 rounded-full w-full mt-8"></div>
                    <div className="h-3 bg-black/5 rounded-full w-full"></div>
                    <div className="h-3 bg-black/5 rounded-full w-3/4"></div>
                  </div>

                  <div className="absolute bottom-8 left-8 right-8 flex items-center justify-between pt-6 border-t border-black/10">
                    <div style={{
                      fontSize: '10px',
                      color: 'rgba(0, 0, 0, 0.3)',
                      fontWeight: 500,
                    }}>
                      Page 1 of 14
                    </div>
                    <Image src="/logos/vylo.png" alt="VYLO" width={60} height={20} className="opacity-30" />
                  </div>
                </div>
              </div>

              <p style={{
                fontSize: '13px',
                color: 'rgba(0, 0, 0, 0.4)',
                marginTop: '16px',
                textAlign: 'center',
                fontWeight: 400,
              }}>
                12-15 pages • Video links embedded • Shareable with college coaches
              </p>
            </motion.div>
          </div>
        </div>
      </section>

      {/* PARENT TESTIMONIALS */}
      <section style={{ padding: '160px 0', background: '#FAFAF9' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20 max-w-[700px]"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Family Perspectives
            </h2>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {[
              {
                quote: "The quarterly reports changed our recruiting conversations completely. College coaches could see documented technical progression, not just tournament results. The level of detail was extraordinary—exactly what they needed to evaluate commitment.",
                name: 'Jennifer Martinez',
                detail: 'Son committed to Duke University',
                photo: '/photos/parent-jennifer.jpg',
              },
              {
                quote: "Sophie went from UTR 10.2 to 12.7 in 14 months. The technical refinements were systematic and measurable. Every quarterly report showed specific improvements we could track. Stanford's recruiting coordinator referenced three separate video analyses from her reports.",
                name: 'Michael Chen',
                detail: 'Daughter committed to Stanford University',
                photo: '/photos/parent-michael.jpg',
              },
              {
                quote: "VYLO prepared our son for the ATP circuit, not just college tennis. The coaching philosophy focused on professional-level preparation. After 18 months at VYLO, he was ready for the transition to tour competition.",
                name: 'Sarah Thompson',
                detail: 'Son competing on ATP Challenger circuit',
                photo: '/photos/parent-sarah.jpg',
              },
            ].map((testimonial, i) => (
              <motion.div
                key={i}
                className="bg-white p-10 rounded-lg"
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.15, duration: 0.8 }}
                style={{
                  boxShadow: '0 4px 32px rgba(0, 0, 0, 0.04)',
                }}
              >
                <p style={{
                  fontSize: '15px',
                  lineHeight: 1.8,
                  color: 'rgba(0, 0, 0, 0.7)',
                  marginBottom: '32px',
                  fontWeight: 400,
                }}>
                  "{testimonial.quote}"
                </p>

                <div className="flex items-center gap-4">
                  <div className="w-12 h-12 rounded-full bg-black/5 flex items-center justify-center overflow-hidden">
                    <div style={{
                      fontSize: '18px',
                      fontWeight: 600,
                      color: 'rgba(0, 0, 0, 0.3)',
                    }}>
                      {testimonial.name.split(' ').map(n => n[0]).join('')}
                    </div>
                  </div>
                  <div>
                    <div style={{
                      fontSize: '15px',
                      fontWeight: 600,
                      color: '#111111',
                      marginBottom: '2px',
                    }}>
                      {testimonial.name}
                    </div>
                    <div style={{
                      fontSize: '13px',
                      color: 'rgba(0, 0, 0, 0.5)',
                      fontWeight: 400,
                    }}>
                      {testimonial.detail}
                    </div>
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* FACILITIES CAROUSEL */}
      <section style={{ padding: '160px 0', background: '#FFFFFF' }}>
        <div className="max-w-[1440px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20 max-w-[700px]"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Training Environment
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              Purpose-built facilities in Laguna Beach, California. Hardcourt surfaces,
              video analysis room, strength lab, athlete housing.
            </p>
          </motion.div>

          <div className="relative">
            <div className="relative aspect-[16/10] overflow-hidden rounded-lg">
              <AnimatePresence mode="wait">
                <motion.img
                  key={carouselIndex}
                  src={facilityImages[carouselIndex].src}
                  alt={facilityImages[carouselIndex].alt}
                  className="w-full h-full object-cover"
                  initial={{ opacity: 0, scale: 1.05 }}
                  animate={{ opacity: 1, scale: 1 }}
                  exit={{ opacity: 0, scale: 0.95 }}
                  transition={{ duration: 0.6, ease: [0.16, 1, 0.3, 1] }}
                />
              </AnimatePresence>

              <div className="absolute bottom-0 left-0 right-0 p-8 bg-gradient-to-t from-black/80 to-transparent">
                <p style={{
                  fontFamily: "'Manrope', sans-serif",
                  fontSize: '18px',
                  fontWeight: 500,
                  color: '#FFFFFF',
                  letterSpacing: '-0.01em',
                }}>
                  {facilityImages[carouselIndex].caption}
                </p>
              </div>
            </div>

            <button
              onClick={prevSlide}
              className="absolute left-4 top-1/2 -translate-y-1/2 w-12 h-12 bg-white/90 hover:bg-white rounded-full flex items-center justify-center transition-all"
              style={{ boxShadow: '0 4px 24px rgba(0, 0, 0, 0.15)' }}
            >
              <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M12 4L6 10L12 16" stroke="#111111" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
            </button>
            <button
              onClick={nextSlide}
              className="absolute right-4 top-1/2 -translate-y-1/2 w-12 h-12 bg-white/90 hover:bg-white rounded-full flex items-center justify-center transition-all"
              style={{ boxShadow: '0 4px 24px rgba(0, 0, 0, 0.15)' }}
            >
              <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M8 4L14 10L8 16" stroke="#111111" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
            </button>

            <div className="flex justify-center gap-2 mt-8">
              {facilityImages.map((_, i) => (
                <button
                  key={i}
                  onClick={() => setCarouselIndex(i)}
                  className="w-2 h-2 rounded-full transition-all"
                  style={{
                    background: i === carouselIndex ? '#111111' : 'rgba(0, 0, 0, 0.2)',
                    width: i === carouselIndex ? '32px' : '8px',
                  }}
                />
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* LONG ARC JOURNEY TIMELINE */}
      <section style={{ padding: '180px 0 160px', background: '#FAFAF9', position: 'relative', overflow: 'hidden' }}>
        <div className="max-w-[1440px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-24 max-w-[800px] mx-auto text-center"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Development Timeline
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(0, 0, 0, 0.6)',
              fontWeight: 400,
            }}>
              Elite development operates on a multi-year arc. Technical refinement,
              competitive maturity, and physical development compound progressively.
            </p>
          </motion.div>

          <div className="relative">
            <motion.div
              className="absolute left-0 md:left-1/2 top-0 bottom-0 w-0.5 bg-black/10 hidden md:block"
              initial={{ scaleY: 0 }}
              whileInView={{ scaleY: 1 }}
              viewport={{ once: true }}
              transition={{ duration: 1.5, ease: [0.16, 1, 0.3, 1] }}
              style={{ transformOrigin: 'top' }}
            />

            <div className="relative" style={{ minHeight: '800px' }}>
              <MilestoneCard
                milestone={{
                  year: 'Year 1',
                  phase: 'Foundation',
                  timeframe: 'Months 1-12',
                  desc: 'Technical baseline assessment. Stroke mechanics refinement. Training rhythm establishment. Initial competitive exposure.',
                  position: { left: '0%', top: '0%' },
                  mobileOrder: 1,
                }}
                index={0}
              />
              <MilestoneCard
                milestone={{
                  year: 'Year 2',
                  phase: 'Development',
                  timeframe: 'Months 13-24',
                  desc: 'Advanced pattern implementation. Increased match volume. Physical capacity building. Mental performance integration.',
                  position: { left: '54%', top: '22%' },
                  mobileOrder: 2,
                }}
                index={1}
              />
              <MilestoneCard
                milestone={{
                  year: 'Year 3',
                  phase: 'Refinement',
                  timeframe: 'Months 25-36',
                  desc: 'Tactical sophistication development. High-level competitive exposure. Recruiting process activation. Performance optimization.',
                  position: { left: '0%', top: '44%' },
                  mobileOrder: 3,
                }}
                index={2}
              />
              <MilestoneCard
                milestone={{
                  year: 'Year 4',
                  phase: 'Transition',
                  timeframe: 'Months 37-48',
                  desc: 'College placement or professional pathway finalization. Peak competitive readiness. Maximum physical development. Complete system mastery.',
                  position: { left: '54%', top: '66%' },
                  mobileOrder: 4,
                }}
                index={3}
              />
            </div>

            <div className="md:hidden space-y-8 mt-12">
              {[
                {
                  year: 'Year 1',
                  phase: 'Foundation',
                  timeframe: 'Months 1-12',
                  desc: 'Technical baseline assessment. Stroke mechanics refinement. Training rhythm establishment. Initial competitive exposure.',
                },
                {
                  year: 'Year 2',
                  phase: 'Development',
                  timeframe: 'Months 13-24',
                  desc: 'Advanced pattern implementation. Increased match volume. Physical capacity building. Mental performance integration.',
                },
                {
                  year: 'Year 3',
                  phase: 'Refinement',
                  timeframe: 'Months 25-36',
                  desc: 'Tactical sophistication development. High-level competitive exposure. Recruiting process activation. Performance optimization.',
                },
                {
                  year: 'Year 4',
                  phase: 'Transition',
                  timeframe: 'Months 37-48',
                  desc: 'College placement or professional pathway finalization. Peak competitive readiness. Maximum physical development. Complete system mastery.',
                },
              ].map((milestone, i) => (
                <motion.div
                  key={i}
                  className="bg-white p-8 rounded-lg"
                  initial={{ opacity: 0, y: 30 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: i * 0.15, duration: 0.8 }}
                  style={{
                    boxShadow: '0 4px 24px rgba(0, 0, 0, 0.04)',
                  }}
                >
                  <div style={{
                    fontSize: '32px',
                    fontWeight: 600,
                    color: '#111111',
                    marginBottom: '8px',
                    letterSpacing: '-0.02em',
                  }}>
                    {milestone.year}
                  </div>
                  <h3 style={{
                    fontSize: '20px',
                    fontWeight: 600,
                    color: '#111111',
                    marginBottom: '6px',
                  }}>
                    {milestone.phase}
                  </h3>
                  <div style={{
                    fontSize: '11px',
                    fontWeight: 600,
                    letterSpacing: '0.12em',
                    textTransform: 'uppercase',
                    color: 'rgba(0, 0, 0, 0.4)',
                    marginBottom: '16px',
                  }}>
                    {milestone.timeframe}
                  </div>
                  <p style={{
                    fontSize: '15px',
                    lineHeight: 1.7,
                    color: 'rgba(0, 0, 0, 0.65)',
                    fontWeight: 400,
                  }}>
                    {milestone.desc}
                  </p>
                </motion.div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* FOUNDERS SECTION */}
      <section style={{ padding: '160px 0', background: '#FAFAF9' }}>
        <div className="max-w-[1200px] mx-auto px-8 md:px-12">
          <motion.div
            className="text-center mb-24"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <div style={{
              fontSize: '11px',
              fontWeight: 600,
              letterSpacing: '0.12em',
              textTransform: 'uppercase',
              color: 'rgba(0, 0, 0, 0.4)',
              marginBottom: '16px',
            }}>
              Leadership
            </div>
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 48px)',
              fontWeight: 600,
              color: '#111111',
              letterSpacing: '-0.02em',
            }}>
              The Co-Founders
            </h2>
          </motion.div>

          <div className="grid grid-cols-1 lg:grid-cols-2 gap-16">
            {/* Andrew Mateljan */}
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.8 }}
            >
              <div className="bg-white p-12 rounded-sm" style={{
                boxShadow: '0 4px 24px rgba(0, 0, 0, 0.04)',
              }}>
                <div className="aspect-[3/4] mb-8 overflow-hidden rounded-sm bg-gray-100 relative">
                  <Image
                    src="https://qtrypzzcjebvfcihiynt.supabase.co/storage/v1/object/public/base44-prod/public/690bf75d8cd1b88fbac92ad3/b542dd0d0_AndrewMateljanPic.png"
                    alt="Andrew Mateljan"
                    fill
                    className="object-cover object-top"
                  />
                </div>

                <h3 style={{
                  fontSize: '28px',
                  fontWeight: 600,
                  color: '#111111',
                  marginBottom: '8px',
                  letterSpacing: '-0.01em',
                }}>
                  Andrew Mateljan
                </h3>

                <div style={{
                  fontSize: '15px',
                  color: 'rgba(0, 0, 0, 0.5)',
                  marginBottom: '24px',
                  fontWeight: 500,
                }}>
                  Co-Founder & ATP Tour Coach
                </div>

                <div className="h-px w-12 bg-black/10 mb-6" />

                <div className="space-y-4 mb-8">
                  <div style={{
                    fontSize: '15px',
                    lineHeight: 1.7,
                    color: 'rgba(0, 0, 0, 0.65)',
                    fontWeight: 400,
                  }}>
                    25 years developing championship-level players. Four active ATP-ranked professionals currently under coaching.
                  </div>

                  <div style={{
                    fontSize: '15px',
                    lineHeight: 1.7,
                    color: 'rgba(0, 0, 0, 0.65)',
                    fontWeight: 400,
                  }}>
                    International experience across Spain, Croatia, and Norway. ATP Masters 1000 tournament coaching at Indian Wells.
                  </div>
                </div>

                <div className="space-y-3 pt-6 border-t border-black/6">
                  <div className="flex items-start gap-3">
                    <div style={{
                      width: '6px',
                      height: '6px',
                      borderRadius: '50%',
                      background: '#F26522',
                      marginTop: '8px',
                      flexShrink: 0,
                    }} />
                    <div style={{
                      fontSize: '14px',
                      color: 'rgba(0, 0, 0, 0.6)',
                      lineHeight: 1.6,
                    }}>
                      Four active ATP-ranked players
                    </div>
                  </div>
                  <div className="flex items-start gap-3">
                    <div style={{
                      width: '6px',
                      height: '6px',
                      borderRadius: '50%',
                      background: '#F26522',
                      marginTop: '8px',
                      flexShrink: 0,
                    }} />
                    <div style={{
                      fontSize: '14px',
                      color: 'rgba(0, 0, 0, 0.6)',
                      lineHeight: 1.6,
                    }}>
                      Highest player ranking: ATP #133
                    </div>
                  </div>
                  <div className="flex items-start gap-3">
                    <div style={{
                      width: '6px',
                      height: '6px',
                      borderRadius: '50%',
                      background: '#F26522',
                      marginTop: '8px',
                      flexShrink: 0,
                    }} />
                    <div style={{
                      fontSize: '14px',
                      color: 'rgba(0, 0, 0, 0.6)',
                      lineHeight: 1.6,
                    }}>
                      100,000+ followers across Fit4Tennis platforms
                    </div>
                  </div>
                </div>
              </div>
            </motion.div>

            {/* Kevin Jackson */}
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.8, delay: 0.2 }}
            >
              <div className="bg-white p-12 rounded-sm" style={{
                boxShadow: '0 4px 24px rgba(0, 0, 0, 0.04)',
              }}>
                <div className="aspect-[3/4] mb-8 overflow-hidden rounded-sm bg-gray-100 relative">
                  <Image
                    src="https://qtrypzzcjebvfcihiynt.supabase.co/storage/v1/object/public/base44-prod/public/690bf75d8cd1b88fbac92ad3/0505f2a39_kevinJacksonPic.png"
                    alt="Kevin Jackson"
                    fill
                    className="object-cover object-top"
                  />
                </div>

                <h3 style={{
                  fontSize: '28px',
                  fontWeight: 600,
                  color: '#111111',
                  marginBottom: '8px',
                  letterSpacing: '-0.01em',
                }}>
                  Kevin Jackson
                </h3>

                <div style={{
                  fontSize: '15px',
                  color: 'rgba(0, 0, 0, 0.5)',
                  marginBottom: '24px',
                  fontWeight: 500,
                }}>
                  Co-Founder & NCAA Recruitment Director
                </div>

                <div className="h-px w-12 bg-black/10 mb-6" />

                <div className="space-y-4 mb-8">
                  <div style={{
                    fontSize: '15px',
                    lineHeight: 1.7,
                    color: 'rgba(0, 0, 0, 0.65)',
                    fontWeight: 400,
                  }}>
                    Specialized expertise in NCAA Division I placement. Over 20 successful collegiate commitments across top-tier programs.
                  </div>

                  <div style={{
                    fontSize: '15px',
                    lineHeight: 1.7,
                    color: 'rgba(0, 0, 0, 0.65)',
                    fontWeight: 400,
                  }}>
                    Systematic approach to athletic development and recruitment strategy. Tournament preparation and competitive pathway design.
                  </div>
                </div>

                <div className="space-y-3 pt-6 border-t border-black/6">
                  <div className="flex items-start gap-3">
                    <div style={{
                      width: '6px',
                      height: '6px',
                      borderRadius: '50%',
                      background: '#F26522',
                      marginTop: '8px',
                      flexShrink: 0,
                    }} />
                    <div style={{
                      fontSize: '14px',
                      color: 'rgba(0, 0, 0, 0.6)',
                      lineHeight: 1.6,
                    }}>
                      20+ Division I placements
                    </div>
                  </div>
                  <div className="flex items-start gap-3">
                    <div style={{
                      width: '6px',
                      height: '6px',
                      borderRadius: '50%',
                      background: '#F26522',
                      marginTop: '8px',
                      flexShrink: 0,
                    }} />
                    <div style={{
                      fontSize: '14px',
                      color: 'rgba(0, 0, 0, 0.6)',
                      lineHeight: 1.6,
                    }}>
                      Zero major injuries across 147 athlete-months tracked
                    </div>
                  </div>
                  <div className="flex items-start gap-3">
                    <div style={{
                      width: '6px',
                      height: '6px',
                      borderRadius: '50%',
                      background: '#F26522',
                      marginTop: '8px',
                      flexShrink: 0,
                    }} />
                    <div style={{
                      fontSize: '14px',
                      color: 'rgba(0, 0, 0, 0.6)',
                      lineHeight: 1.6,
                    }}>
                      USPTA Elite Professional certification
                    </div>
                  </div>
                </div>
              </div>
            </motion.div>
          </div>
        </div>
      </section>

      {/* FAQ SECTION */}
      <section style={{ padding: '160px 0', background: '#FFFFFF' }}>
        <div className="max-w-[900px] mx-auto px-8 md:px-12">
          <motion.div
            className="mb-20"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(36px, 4vw, 56px)',
              fontWeight: 600,
              color: '#111111',
              marginBottom: '24px',
              letterSpacing: '-0.02em',
            }}>
              Application Process
            </h2>
          </motion.div>

          <div className="space-y-6">
            {[
              {
                question: 'What is the application timeline?',
                answer: 'Applications open September 1st for the following January cohort. Initial review occurs October 1-15. On-site evaluations scheduled November 1-30. Final decisions communicated by December 15th.',
              },
              {
                question: 'What does the evaluation process involve?',
                answer: 'Three-day on-site assessment at Laguna Beach facilities. Technical evaluation with lead coach. Match play observation. Physical assessment with strength staff. Family interview with program director.',
              },
              {
                question: 'What is the annual program investment?',
                answer: 'Full-time enrollment: $98,000 annually. Includes all coaching, facility access, strength training, video analysis, academic coordination, and athlete housing. Tournament travel, equipment, and personal expenses separate.',
              },
              {
                question: 'Can athletes train part-time?',
                answer: 'No. VYLO operates exclusively as a full-time development program. The training architecture requires consistent daily presence for technical progression and competitive development.',
              },
              {
                question: 'How does academic coordination work?',
                answer: 'Athletes complete college-prep curriculum through accredited online platform. Daily study hall with academic coordinator. Individual tutoring available. NCAA eligibility maintained throughout enrollment.',
              },
              {
                question: 'What happens if an athlete is not accepted?',
                answer: 'Families receive detailed written feedback outlining specific developmental areas and timeline recommendations. Athletes may reapply for future cohorts after addressing identified areas. Alternative program recommendations provided when appropriate.',
              },
            ].map((faq, i) => (
              <motion.div
                key={i}
                className="border-b border-black/10 pb-6"
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.1, duration: 0.8 }}
              >
                <h3 style={{
                  fontSize: '20px',
                  fontWeight: 600,
                  color: '#111111',
                  marginBottom: '12px',
                  letterSpacing: '-0.01em',
                }}>
                  {faq.question}
                </h3>
                <p style={{
                  fontSize: '16px',
                  lineHeight: 1.7,
                  color: 'rgba(0, 0, 0, 0.6)',
                  fontWeight: 400,
                }}>
                  {faq.answer}
                </p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* FINAL CTA */}
      <section style={{ padding: '120px 0', background: '#0A0A0A' }}>
        <div className="max-w-[800px] mx-auto px-8 md:px-12 text-center">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 1, ease: [0.16, 1, 0.3, 1] }}
          >
            <h2 style={{
              fontFamily: "'Manrope', sans-serif",
              fontSize: 'clamp(32px, 4vw, 48px)',
              fontWeight: 600,
              color: '#FFFFFF',
              marginBottom: '32px',
              letterSpacing: '-0.02em',
              lineHeight: 1.2,
            }}>
              January 2026 Cohort
            </h2>
            <p style={{
              fontSize: '17px',
              lineHeight: 1.8,
              color: 'rgba(255, 255, 255, 0.7)',
              marginBottom: '48px',
              fontWeight: 400,
            }}>
              Applications open September 1, 2025. Ten positions available.
            </p>
            <Link
              href="/book"
              className="inline-flex items-center gap-4 px-10 py-5 bg-[#F26522] hover:bg-[#E85A10] transition-all duration-300"
              style={{
                fontFamily: "'Manrope', sans-serif",
                fontSize: '14px',
                fontWeight: 600,
                letterSpacing: '0.08em',
                textTransform: 'uppercase',
                color: '#FFFFFF',
              }}
            >
              Submit Application
              <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                <path d="M1 9H17M17 9L9 1M17 9L9 17" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
            </Link>
          </motion.div>
        </div>
      </section>

      {/* FOOTER */}
      <footer style={{ padding: '80px 0 60px', background: '#FAFAF9', borderTop: '1px solid rgba(0, 0, 0, 0.06)' }}>
        <div className="max-w-[1440px] mx-auto px-8 md:px-12">
          <div className="grid grid-cols-1 md:grid-cols-3 gap-12 mb-16">
            <div>
              <Image src="/logos/vylo.png" alt="VYLO" width={140} height={46} className="mb-6 opacity-80" />
              <p style={{
                fontSize: '14px',
                lineHeight: 1.7,
                color: 'rgba(0, 0, 0, 0.5)',
                fontWeight: 400,
              }}>
                Professional tennis development in Laguna Beach, California.
              </p>
            </div>
            <div>
              <h4 style={{
                fontSize: '13px',
                fontWeight: 600,
                letterSpacing: '0.08em',
                textTransform: 'uppercase',
                color: 'rgba(0, 0, 0, 0.4)',
                marginBottom: '16px',
              }}>
                Contact
              </h4>
              <div style={{ fontSize: '15px', lineHeight: 1.9, color: 'rgba(0, 0, 0, 0.6)', fontWeight: 400 }}>
                <div>info@vylo.tennis</div>
                <div>(949) 555-0100</div>
                <div className="mt-3">Laguna Beach, CA 92651</div>
              </div>
            </div>
            <div>
              <h4 style={{
                fontSize: '13px',
                fontWeight: 600,
                letterSpacing: '0.08em',
                textTransform: 'uppercase',
                color: 'rgba(0, 0, 0, 0.4)',
                marginBottom: '16px',
              }}>
                Partnership
              </h4>
              <p style={{
                fontSize: '14px',
                lineHeight: 1.7,
                color: 'rgba(0, 0, 0, 0.5)',
                marginBottom: '12px',
                fontWeight: 400,
              }}>
                Training conducted in partnership with Laguna Beach Tennis Academy
              </p>
              <Link
                href="https://lbtennis.com"
                className="text-sm hover:opacity-70 transition-opacity"
                style={{
                  color: 'rgba(0, 0, 0, 0.6)',
                  fontWeight: 500,
                }}
              >
                lbtennis.com →
              </Link>
            </div>
          </div>

          <div className="pt-8 border-t border-black/6 flex flex-col md:flex-row justify-between items-center gap-4">
            <p style={{
              fontSize: '13px',
              color: 'rgba(0, 0, 0, 0.4)',
              fontWeight: 400,
            }}>
              © 2025 VYLO Tennis. All rights reserved.
            </p>
            <div className="flex gap-6">
              <Link href="/privacy" style={{
                fontSize: '13px',
                color: 'rgba(0, 0, 0, 0.5)',
                fontWeight: 500,
              }} className="hover:opacity-70 transition-opacity">
                Privacy Policy
              </Link>
              <Link href="/terms" style={{
                fontSize: '13px',
                color: 'rgba(0, 0, 0, 0.5)',
                fontWeight: 500,
              }} className="hover:opacity-70 transition-opacity">
                Terms of Service
              </Link>
            </div>
          </div>
        </div>
      </footer>
    </div>
  )
}
