# Cursor config (this project)

- **MCP config:** `.cursor/mcp.json` — defines **vercel** and **activecampaign**. File is gitignored (secrets not committed).
- **Get both working:** Follow **[.cursor/MCP_CHECKLIST.md](.cursor/MCP_CHECKLIST.md)** step by step (remove Marketplace duplicates, Vercel login, ActiveCampaign token, then restart Cursor).
- **Compound learnings:** `.cursor/compound/` — see that folder’s README.
